# CONTEXT.md — полный контекст проекта (на 2026-07-04)

> Сводка всей проделанной работы и текущего состояния. Для продолжения работы
> в новой сессии: прочитать этот файл + CLAUDE.md (ТЗ) + DECISIONS.md (решения)
> + ROADMAP.md (что дальше). Источник правды по спецификации — CLAUDE.md.

---

## 1. Что это

**Финансовый контур Brave Talents** — внутренняя система, заменяющая согласование
платежей в WhatsApp и ручную сборку управленческой отчётности:

- **Система А**: заявки на оплату → маршруты согласования → казначейский реестр
  (приоритизация при дефиците) → отметка «оплачено» бухгалтером.
- **Система Б**: автоматический учёт — поступление клиента дробится на
  НДС (3098) / себестоимость (7366) / маржу (6890), проектные балансы
  Клиент → Проект → Получатель, бюджет 6890 план-факт, депозиты/резервы.
- **Проекты/сделки**: продажники заносят сделки с блогерами из базы цен,
  проджекты ведут исполнение, статусы оплат и дебиторка видны онлайн.

Дальний план CFO: это ядро, к которому позже подключится CRM (этапы сделок) —
через API, проект/клиент — связующие сущности (DECISIONS §13).

## 2. Стек и запуск

Next.js 15.5 (App Router, TS) · Prisma 6 · PostgreSQL 16 (Docker) · Tailwind 4.

```bash
npm run db:up        # Postgres в Docker (нужен запущенный Docker Desktop)
npx prisma migrate deploy   # применить миграции (безопасно)
npm run db:seed      # справочники + демо (идемпотентен, безопасен на живой БД)
npm run dev          # http://localhost:3000
npm run test:accounting     # тесты денежного движка (25 шт.)
npm run bloggers:import -- "путь/к/файлу.xlsx"   # импорт базы блогеров
```

⚠️ Windows: перед `prisma generate` останавливать dev-сервер (держит DLL).
Пароль всех тестовых пользователей: **password123**.

## 3. Люди и роли (сид)

| Роль | Кто |
|---|---|
| CFO («видит всё», админ) | sanzhar.assylbek@ |
| Гл. бухгалтер (коллегия) | zhadyra.kasymbek@ |
| Бухгалтеры | symbat.otesh@, elnura.ordabaeva@ |
| Опер. директор (коллегия) | ainur.abduali@ |
| Рук. продаж (ком-директор) | azhar.rakhat@ |
| **Продажники** (заносят сделки) | meruyert.nurbolganova@, aigerim.amangeldi@, ayaulym.khairullina@, islom.yunusmetov@ |
| Рук. проджектов (дир. блогеров) | rakhima.turzhanova@ |
| **Проджекты** (пара к продажнику) | gulzira.dastanbek@, azhar.iklassova@, ainura.imanaliyeva@, erke.myrzabek@, alina.faber@ |
| Заявители | blogger.staff@, dimash@, ramzat@, aisulu@, tima@, zhaskairat@, office.manager@, it.manager@ |

(все @bravetalents.com). ❓ Открытый вопрос: «Рахмина»-продажник была указана
с почтой Рахимы Туржановой — вероятно опечатка; ждём реальную почту.

## 4. Хронология работ (коммиты)

| Коммит | Что сделано |
|---|---|
| c488e98…d03b4ee | Каркас MVP (этапы 1–8 ТЗ): заявки, согласование, казначейство, учёт, бюджет, админка, импорт Kaspi |
| ffb2209 | **Заявки v2 + полный аудит**: срочность вместо приоритета (1/2-3/5 раб. дней), форма гонорара блогера (договор × % = сумма, пред/постоплата, «услуга оказана» → обязательный акт), черновик/отправка/доработка с комментарием согласующего, вложения по категориям, фильтры списка. Аудит: claim-guard от двойного разнесения и двойной оплаты, атомарные переходы согласования, факт бюджета за период, XSS вложений закрыт, Next.js 15.1.4→15.5.20 (CVE), убран захардкоженный entity, доступ коллегии к реестру, kaspi-парсер (запятая ×100), тесты 15→21 |
| b8f2156 | DECISIONS §13: целевая картина, задел под CRM |
| 8caedbe | **Вкладка «Проекты»**: роль ACCOUNT_MANAGER, 4 раздела (Блогеры/Продакшн/Ивенты/Спец), карточка проекта со сметой (версии + автопересчёт разнесений ADJUSTMENT-проводками), тайминги заявки («от подачи до оплаты»), тесты →25 |
| ecc97c6 | **Этап «Сделка»** (DECISIONS §14): пары продажник+проджект (PROJECT_MANAGER), даты реализации/завершения, экономика сделки (НДС 12/112 авто), продакшн-резерв как строка себестоимости + depositAmount |
| 735918f | **База блогеров из Excel**: 57 блогеров × 400 опций из «OLD New Статистика Brave (1).xlsx» (листы Brave Price + Non-standart price), себес/налог/себес-с-налогом (gross-up), опции дропдауном в сделке с автоценой и скидкой, ссылка на аккаунт |
| 2121e60 | **Карточка проекта — деньги**: поступило/дебиторка в списке и карточке, итоги вкладки, прогресс-бары, «→ Заявка на оплату» из получателя (префилл), закрытие/переоткрытие проекта |
| (2026-07-04) | **Форма «Создать проект» v2** (DECISIONS §14.5): модальное окно вместо блока, сквозной номер проекта (Project.number, система присваивает max+1), клиент только из справочника (поиск + окно добавления), «Запланированная дата завершения», продакшн-резерв по каждой строке блогер × опция (EstimateLine.reserveAmount, Σ → depositAmount), «Проекты» в меню после «Главной», форма пока только в разделе «Блогеры» |
| (2026-07-04) | **Карточка клиента + доработки формы** (DECISIONS §14.6): окно «Новый клиент» с реквизитами (юр. название, ИП/ТОО/АО/ЧК, местная/иностранная, БИН, IBAN, банк из списка БВУ РК, КБЕ авто 17/19/27/29), «Дата утверждения проекта» (бывш. реализации), смена блогера сохраняет опцию (гонорар — по прайсу нового), расчёт: + колонки «Продакшн» и «Маржа, %» |

## 5. Ключевые доменные правила

- Деньги: **BigInt тиыны** (1 ₸ = 100 тиын), никогда float. Балансы — всегда
  агрегат журнала Transaction (остатки нигде не хранятся).
- **Конфиденциальность (§10)** — в SQL, не в UI: заявитель видит свои заявки;
  проект видят владелец (продажник) + проджект + департамент; «видят всё»
  только CFO/бухгалтерия (canSeeEverything). Любой новый запрос обязан
  повторять этот скоуп.
- Переходы статусов — **claim-guard** (`updateMany where status=...`) внутри
  `$transaction`: двойной клик/гонка не задваивают деньги и статусы.
- Смета — источник правды; новая версия **автоматически пере-разносит**
  полученные поступления (ADJUSTMENT-проводки на дельты, DECISIONS §1.1).
- Прайс блогера: опции с названиями из базы; **скидка = себес-с-налогом −
  заполненный гонорар**; ограничений на скидку нет (решение CFO).
- Себестоимость Influence-сделки = гонорары блогеров + продакшн-резервы;
  резерв считается **по каждой строке (блогер × опция)** —
  EstimateLine.reserveAmount, Σ строк = EstimateVersion.depositAmount.
- Номер проекта — сквозной по компании, присваивает система (Project.number,
  уникален в entity); клиент — только из справочника (создание — отдельным
  действием createClient, дубликаты по имени не плодим).
- Жизненный цикл заявки: Черновик → На согласовании (по ступеням) →
  Одобрена → В реестре → Оплачена; + На доработке (возврат автору),
  Отклонена, Отменена. Всё в AuditLog, тайминги видны на карточке.

## 6. Карта кода

```
src/lib/
  auth/        сессии, RBAC (permissions.ts: роли, canSeeEverything, NAV_ITEMS)
  requests/    заявки: service (жизненный цикл), actions, queries, status (словари), urgency
  estimates/   сметы: service (версии + пересчёт разнесений), actions
  projects/    вкладка Проекты: queries (скоуп!), actions (сделка, закрытие)
  treasury/    реестр/календарь/оплата (markPaid — атомарный)
  accounting/  проводки (posting.ts), балансы, отчёт, queries
  budget/      план-факт 6890 (факт из PAYOUT за период)
  import/      Kaspi-выписка (kaspi.ts на xlsx — мигрировать на exceljs)
src/app/(app)/ страницы: requests, projects, approvals, treasury, payments,
               accounting/*, admin
scripts/       test-accounting.ts (25 тестов), import-bloggers.ts, demo-scenario.ts
```

## 7. Что проверено

- Тесты движка: **25/25** (разнесение, округление, анти-задвоение оплаты и
  разнесения, пересчёт по новой смете до тиына).
- `tsc --noEmit` чист; прод-сборка Next 15.5.20 проходит (22 маршрута).
- Смоуки по ролям: продажник/проджект/бухгалтер/CFO/заявитель — доступы и
  рендер ключевых экранов.

## 8. Открытые хвосты (подробно — ROADMAP.md)

1. **Депозиты/резервы не копятся** — depositAmount пишется, но при разнесении
   поступлений резерв не отщепляется в депозит-леджер; дашборд «копилок» пуст.
   ← рекомендуемый следующий блок.
2. Вкладка Проекты: поиск/сортировка, редактирование проекта, этапы жизни.
3. Импорт выписки: дедупликация повторной загрузки; xlsx → exceljs (уязвимости).
4. Админка: редактирование пользователей/маршрутов (сейчас только создание).
5. SLA-отчёт по таймингам согласования (данные уже пишутся).
6. Цены-диапазоны таргета из базы блогеров («700000/1100000») — пропущены;
   можно завести как «Таргет 7 дней»/«Таргет месяц».
7. Уведомления согласующим; прод: volume для uploads/, бэкапы, SSO.
